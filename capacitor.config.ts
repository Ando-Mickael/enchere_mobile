import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'mg.ando.myproject',
  appName: 'myProject',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
